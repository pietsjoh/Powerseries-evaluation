"""Contains the base file data_import, which loads the data.
Later this will be maybe split into 1 base class, and inherit 2 different lab setups (qlab2, qlab5).
sort_data sorts the files of the data directory by diameter and by used grating (fine, non-fine) into the sorted_data directory.
"""